// What an error screen says (DESIGN.md §7). The label is the window title and
// the Silkscreen line above the heading.
export interface ErrorScreenCopy {
  label: string
  title: string
  body: string
}
